# Venom profile: ONNX local LLM runtime (CPU-only)
#
# Use this profile on hosts without dedicated GPU inference.
# It keeps ONNX Runtime GenAI support but avoids CUDA/Torch stack.
#
# Install with:
#   pip install -r requirements-profile-onnx-cpu.txt

-r requirements-profile-api.txt

# ONNX CPU runtime stack for local LLM inference.
# Shares the same ONNX floor as the GPU profile so the variant only differs
# in runtime backend, not in model-format support.
onnxruntime
onnxruntime-genai
onnx>=1.21.0
onnx-ir>=0.1.16

# Intentionally excluded here (heavy GPU/torch chain):
# - onnxruntime-gpu
# - onnxruntime-genai-cuda
# - optimum
# - accelerate
