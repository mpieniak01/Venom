# Venom profile: vLLM local runtime
#
# Self-contained overlay for the local vLLM stack on top of the shared runtime base.

-r requirements-runtime-common.txt

# vLLM-specific runtime baseline
pydantic==2.13.4
pydantic-settings==2.10.1
numpy>=2.0.0,<3.0.0
semantic-kernel==1.43.1
openai>=2.24.0,<3.0.0

# vLLM runtime package (provides .venv/bin/vllm)
cbor2==5.9.0
torch==2.11.0
transformers>=5.5.0,<6.0.0
vllm==0.24.0

# Optional quantization backend for multi_runtime profile tuning (217B).
# Linux-only wheels are currently the supported path in this stack.
bitsandbytes>=0.49.0; platform_system == "Linux"
