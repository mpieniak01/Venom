[pytest]
addopts = --dist loadfile
filterwarnings =
    ignore::pydantic.PydanticDeprecatedSince211
    ignore::pydantic.PydanticDeprecatedSince20
    ignore::requests.exceptions.RequestsDependencyWarning
    ignore:coroutine 'AsyncMockMixin\._execute_mock_call' was never awaited:RuntimeWarning
    # Ignorujemy ostrzeżenie PytestUnraisableExceptionWarning (zamykana pętla asyncio
    # dla BaseSubprocessTransport) pojawiające się w testach z ASGITransport/TestClient.
    ignore::pytest.PytestUnraisableExceptionWarning
markers =
    performance: Testy wydajnościowe backendu/Next.
    manual_llm: Testy manualne zależne od lokalnego runtime/modelu LLM (poza automatyczną regresją).
    smoke: Szybkie testy dymne pipeline'u czatu.
    slow_fastlane_exempt: Test jest poprawny, ale celowo wyłączony z szybkiej ścieżki coverage.

# Konfiguracja discovery
norecursedirs =
    .git
    .venv
    node_modules
    web-next
    docs
    docs_dev
    config
    data

testpaths = tests
