# Venom profile: API + web-next integration baseline
#
# Użyj tego profilu, gdy uruchamiasz backend razem z frontendem `web-next`.
# Profil bazuje na API runtime i obecnie nie dokłada własnych pinów ponad
# wspólny backend, bo wszystkie potrzebne zależności są już w bazie.
#
# Instalacja:
#   pip install -r requirements-profile-web.txt
#
# Wymagania frontend (poza pip):
# - Node.js >= 20.9.0 (Next.js 16)
# - npm >= 10.x

-r requirements-profile-api.txt
