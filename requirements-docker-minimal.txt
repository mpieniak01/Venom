# Venom Docker Minimal runtime dependencies
# Intentionally excludes test/type-only packages used in CI tooling.

# Shared runtime base
-r requirements-runtime-common.txt

# Utilities used by runtime code paths
chardet<6
psutil==5.9.8
numpy<2.0.0
