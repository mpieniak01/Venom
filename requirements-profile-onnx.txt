# Venom profile: ONNX local LLM runtime
# Engine overlay for the medium/developer stack.
# This profile intentionally starts from the shared API/runtime base and adds
# only the ONNX-specific engine stack.
#
# Install with:
#   pip install -r requirements-profile-onnx.txt

-c constraints-hf-onnx.txt
-r requirements-profile-api.txt

# ONNX runtime stack for local LLM inference.
# This file keeps only the runtime core for the Linux/WSL path. Legacy HF
# conversion/probe workflows live in `requirements-full.txt`.
onnxruntime-gpu==1.26.0
onnxruntime-genai-cuda==0.12.1
onnx>=1.21.0
onnx-ir>=0.1.16
